version: '3'

services:
  app:
    build:
      context: .
      args:
        APP_DIR: /myapp
    depends_on:
      - db
    volumes:
      - ./:/myapp

  db:
    image: postgres:15
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - db-volume:/var/lib/postgresql/data

volumes:
  db-volume:
